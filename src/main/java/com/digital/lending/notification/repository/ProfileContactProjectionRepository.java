package com.digital.lending.notification.repository;

import com.digital.lending.notification.model.ProfileContactProjection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProfileContactProjectionRepository extends JpaRepository<ProfileContactProjection, String> {
}
